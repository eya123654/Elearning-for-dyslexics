{% extends 'BaseB.html.twig' %}

{% block title %}Edit Cours{% endblock %}

{% block body %}
    <h1>Edit Cours</h1>

    {{ include('cl_back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cours_index') }}">back to list</a>

    {{ include('cl_back/_delete_form.html.twig') }}
{% endblock %}
